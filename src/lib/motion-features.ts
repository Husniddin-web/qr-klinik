/**
 * LazyMotion uchun feature to'plami — asosiy bundle'dan tashqarida,
 * hydration'dan keyin alohida chunk sifatida yuklanadi.
 *
 * `domMax` tanlandi, chunki loyihada kerak:
 *   - layout animatsiyalari (Navbar `layoutId` pill)
 *   - drag (Testimonials ustunlari)
 */
import { domMax } from "framer-motion";

export default domMax;
